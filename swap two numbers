#include <stdio.h>
void swap(int *a,int *b)
{
    int temp;
    temp=*a;
    *a=*b;
    *b=temp;
}
int main()
{
    int a=5,b=2;
    printf("before swapping a=%d and b=%d\n",a,b);
    swap(&a,&b);
    printf("after swaping a=%d and b=%d",a,b);
}
